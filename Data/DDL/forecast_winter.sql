/****** Object:  Table [dbo].[FORECAST_WINTER]    Script Date: 11/16/2018 9:31:40 AM ******/
SET ANSI_NULLS ON
GO

SET QUOTED_IDENTIFIER ON
GO

CREATE TABLE [dbo].[FORECAST_WINTER](
       [ID] [int] IDENTITY(1,1) NOT NULL,
       [SETTLEMENT_INTERVAL] [int] NOT NULL,
       [permutation] [int] NOT NULL,
       [YHAT] [decimal](18, 0) NOT NULL,
PRIMARY KEY CLUSTERED
(
       [ID] ASC

)WITH (STATISTICS_NORECOMPUTE = OFF, IGNORE_DUP_KEY = OFF) ON [PRIMARY]
) ON [PRIMARY]
GO

ALTER TABLE [dbo].[FORECAST_WINTER]  WITH CHECK ADD FOREIGN KEY([permutation])
REFERENCES [dbo].[PERMUTATION] ([id])
GO

ALTER TABLE [dbo].[FORECAST_WINTER]  WITH CHECK ADD FOREIGN KEY([SETTLEMENT_INTERVAL])
REFERENCES [dbo].[SETTLEMENT_INTERVAL] ([id])
GO

 